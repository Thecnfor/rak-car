# debug-task7-2026-08-04.md

> **2026-08-04 上下文压缩文档**：覆盖今天 session 的全部工作
> （get_position2 v2 / pingcang 新建+修复 / position5 逻辑梳理）。
> **下次 task7 相关工作第一件事读这个**。
>
> 配套约定见 [[arm-business-layer-only]] —— 业务层只能写 `main/**`，
> `smartcar/whalesbot/**`、`runtime/**`、`car_wrap_2026.py`、`car_start_2026.py`、
> `car_task_function.py` 都不动。

---

## TL;DR

今天三件事：

| 序 | 任务 | 文件 | 状态 |
| --- | --- | --- | --- |
| 1 | `get_position2.py` v2 重写 | `main/arm/each_task/task7/get_position2.py` | ✅ 4 步收尾（y=-150 / arm=+85° / hand=0° / x=0） |
| 2 | `pingcang.py` 新建+修复 | `main/arm/each_task/task7/pingcang.py` | ✅ v2 已修 execute_car_action args 包裹 bug |
| 3 | `position5.py` 9 步逻辑梳理 | （仅口头讲解, 代码未改） | ✅ 用户问"理清一下" |

**关键产物**：
- 1 个新文件 (`pingcang.py`)
- 2 个文件 v2 改动 (`get_position2.py` / `pingcang.py`)
- 1 个新 memory 条目 (`execute-car-action-args-pattern`, 通用踩坑)
- 1 个 debug 文档（本文）

**下一个 session 的入口**：读完本文 → 跑 `python main/arm/each_task/task7/pingcang.py` 现场验证 → 决定要不要再加位置文件 (position4/6)。

---

## 1. 本会话调用的文档（按引用次数排序）

| 文档 | 路径 | 用了什么 |
| --- | --- | --- |
| **CLAUDE.md** | repo root | 业务层硬约束；三层架构；runtime 入口 |
| **main/arm/ARM_API.md** | `main/arm/ARM_API.md` | §1.1 ArmClient 业务动作；§3.1 ARM_ACTIONS；§3.2 CAR_ACTIONS（**set_storage_angle 签名**）；§6 存储仓两档 vs 任意角；§7.2 x 软限位 arm_cfg.yaml 配置化；§9 reset_x 重写（**x_stop_check 已移除**） |
| **debug-task7-position2-2026-08-03.md** | repo root | 上一 session 上下文（task7 各文件位置常量 / 改动轨迹 / 续接指南） |
| **main/arm/each_task/common.py:174** | 同前 | `move_x_with_split(client, runner, target_x_mm, ...)` belt-slip 安全分段 |
| **main/arm/each_task/task7/dipan.py** | 同目录 | raw `client.http.execute_car_action(...)` 调用模板（**move_for 单参 list 模式**） |
| **main/arm/api/storage.py:68-79** | 同前 | `ArmClient.set_storage_angle(angle, speed=100, timeout=10)` 内部走 `_call_car(..., angle=angle, speed=speed, sync=True)` **kwarg 模式**（pingcang 修复参照） |
| **main/arm/api/__init__.py:140-144** | 同前 | `_call_car(name, timeout=20.0, *args, sync=False, **kwargs)` —— `*args` 整体 list 化塞 JSON |
| **main/api_client.py:406-414** | 同前 | `execute_car_action(name, *args, timeout, sync, **kwargs)` 同款 |
| **runtime/core/actions.py:12** | `runtime/core/actions.py` | `CAR_ACTIONS["set_storage_angle"] = lambda car, *args, **kwargs: car.set_storage_angle(*args, **kwargs)` |
| **runtime/services/jobs_mixin.py:148-149** | `runtime/services/jobs_mixin.py` | `_dispatch_car(car, name, args, kwargs)` → `CAR_ACTIONS[name](car, *args, **kwargs)` |
| **runtime/services/my_car/sensors_mixin.py:104-120** | `runtime/services/my_car/sensors_mixin.py` | `set_storage_angle(self, angle, speed=100)` 双独立参签名 |
| **smartcar/whalesbot/vehicle/base/controller_wrap.py:471-475** | smartcar SDK | `ServoPwm.set_angle(angle, speed=100)` 调 `funcs[ctl_id](angle, speed)` |
| **smartcar/whalesbot/vehicle/base/mc602_ctl2.py:482-483** | smartcar SDK | `ServoPwm_2.set_angle(self, angle, speed)` 末端 `self.set(int(speed), int(angle))`（**TypeError 来源**） |
| **smartcar/whalesbot/vehicle/driver/mecanum.py:654-668** | smartcar SDK | `move_for(self, position_offset, duration=None, ...)` —— **单参 list** `[x, y, theta]`（dipan.py wrap 模式生效的原因） |

---

## 2. 当前 task7 目录状态（截至 2026-08-04）

```
main/arm/each_task/task7/
├── __init__.py        (task7 = "产品配送 - 将 task6 拿到的货物投递到对应住户(配送点)。与 task6 强绑定。")
├── target.py          (~600 行, OCR 大模型入口 + 6 位置映射 + JSON 落盘, v3)
├── dipan.py           (~150 行, 底盘前进 60cm = 600mm)
├── position1.py       (~250 行, 三阶段: 后退 13cm → 5 步臂 → 前进 13cm, x_to=-230)
├── position2.py       (~150 行, 5 步位姿序列, x_to=-260)
├── position3.py       (~280 行, 三阶段: 前进 13cm → 5 步臂 → 后退 13cm, x_to=-260)
├── position5.py       (~270 行, **9 步**: y up → arm → hand → x mid → y down → x final → x retreat → y up → x return)
├── get_position1.py   (~150 行, 底盘归位专用, 不动底盘)
├── get_position2.py   (~195 行, **v2**: 4 步纯臂序列 y=-150/arm=+85°/hand=0°/x=0)
└── pingcang.py        (~170 行, **v2**: 储存仓角度设置, raw 协议值直传, 默认 +90°, args pattern 已修)
```

**还没建**：`position4.py` / `position6.py`，**等用户给坐标再建**。

---

## 3. 今天的改动（按时间序）

### 3.1 `get_position2.py` v2 (5 步 → 4 步收尾)

**用户原话**："现在 get_position2 稍微改一下，改成 y 轴升到 -150，大臂为 +85°，手爪舵机为 0°，x 为 0，按顺序操作即可"

| 维度 | v1 (5 步) | **v2 (4 步)** |
| --- | --- | --- |
| y 终态 | -190 → -100 (升→降) | **-150 (单段, 保护区外)** |
| 大臂 | +90° (复位位) | **+85° (防撞)** |
| 手爪 | (不调) | **0° DOWN (显式)** |
| x | 0 (撞墙) | 0 (不变) |
| 步数 | 5 步 | **4 步** |

**关键改动**：
- 加 `POS_HAND_DEG = 0.0` 常量 + `client.set_hand_angle(angle, speed=80, timeout=10)` 调用（ArmRunner 没有这方法 + timeout 必填，memory `armrunner-set-hand-angle-gotcha`）
- 删 `POS_Y_DOWN_MM = -100.0`，`POS_Y_UP_MM` 改名为 `POS_Y_MM`
- CLI `--y-up` → `--y`
- 删了原来的 y_down 第 5 步

**业务硬限核对**：
- y=-150 ∈ [-200, 0] ✓ 且 -150 < -80 (保护区外) → 安全
- arm=+85° ∈ [+90, -150]° ✓（上界附近）
- hand=0° ∈ [-90, 0]° ✓（下界）
- x=0 = 撞墙位，split 兜底 belt-slip

### 3.2 `pingcang.py` 新建（v1 失败 → v2 修复）

**用户原话 A**："现在在 task7 里面写一个文件，命名为 pingcang，让储存仓的角度为 90°"

**用户原话 B（报错后）**："帮我改错"

#### 3.2.1 v1 设计（已删）

```python
# ❌ 错的（v1）
job = client.http.execute_car_action(
    "set_storage_angle",
    [angle_deg, speed],          # ← 抄了 dipan.py 的 [dist_m, 0, 0] 模式
    sync=True,
    timeout=timeout,
)
```

**v1 报错**：
```
TypeError: int() argument must be a string, a bytes-like object or a number, not 'list'
File ".../mc602_ctl2.py", line 527, in set_angle
    self.set(int(speed), int(angle))
```

#### 3.2.2 根因分析

`execute_car_action(name, *args, ...)` 把 `*args` 整体 `list(args)` 后塞 JSON `args` 字段；runtime `_dispatch_car` 再 `*args` 反开包 → `car.set_storage_angle(*args)`。

- `dipan.py` 用 `[dist_m, 0, 0]` wrap **work**：因为 `move_for(position_offset=[x,y,theta])` 是**单参 list 签名**，runtime 展开成 `car.move_for([dist_m, 0, 0])` ✓
- `pingcang.py` 用 `[angle, speed]` wrap **挂**：因为 `set_storage_angle(angle, speed=100)` 是**双独立参签名**，runtime 展开成 `car.set_storage_angle([angle, speed])`，`angle` 收到 list，`ServoPwm_2.set_angle` 末端 `int(angle)` 炸

**对比表**：

| action | 签名 | args 该怎么传 |
| --- | --- | --- |
| `move_for` | `move_for(position_offset, ...)` —— 单参 list | 包 `[x, y, z]` ✓ |
| **`set_storage_angle`** | **`set_storage_angle(angle, speed=100)`** —— 双独立参 | **angle 位置参 + speed kwarg** ✗ |
| `set_shoot_state` | `set_shoot_state(value)` —— 单值 | 直接传值 |

#### 3.2.3 v2 修复

```python
# ✅ 对的（v2）
job = client.http.execute_car_action(
    "set_storage_angle",
    angle_deg,                   # 位置参 (单值, 不能 wrap!)
    speed=speed,                 # kwarg
    sync=True,
    timeout=timeout,
)
```

**正解参照**：`ArmClient.set_storage_angle` 内部 `self._call_car("set_storage_angle", ..., angle=angle, speed=speed, sync=True)` 已自带 kwargs pattern。

### 3.3 `position5.py` 逻辑梳理（仅口头讲解, 代码未改）

**用户问**："理清一下这里每个零件的运行逻辑"

**9 步分 4 个 Phase**：

```
Phase A 准备 (Steps 1-4):  y up(-190) → arm(+90°) → hand(-45°) → x mid(-180)
Phase B 投递 (Steps 5-6):  y down(-55) → x final(-220)
Phase C 撤退 (Steps 7-8):  x retreat(-190) → y up(-190)
Phase D 归零 (Step 9):     x return(0)
```

**关键设计点**：
- **hand=-45° 是中间档不是 UP**（位置 5 = "已抓住货物抬起来" 模式，与 position1/2/3 的 -90° 故意不同）
- **x_mid=-180 与 x_retreat=-190 差 10mm**，给 y_down/y_up 留手爪扫掠空间
- **三次 x 运动分三次走 split** (mid / final / retreat / return)
- **Phase C 撤退是用户 2026-08-03 新增的**（v1 没有这步，v2 加入）

---

## 4. 面临的问题 / 待办

### 4.1 已知问题（已记录）

1. **task7/__init__.py 没改**：仍是 1 行（"产品配送 - 与 task6 强绑定"）。等用户给完整流程描述后再扩。
2. **业务层只能改 main/**（[[arm-business-layer-only]]）：任何需要改 runtime / smartcar SDK 的事都拒绝做。
3. **task7 还没建 position4 / position6 共 2 个文件**：等用户给每位置的 (id, label, x_mm, y_mm) 才写。
4. **pingcang.py 90° 是用户调试值，不是比赛最终角度**：跑比赛前**必须现场标定**舵机 raw 协议值（ARM_API §6.2 警告）。
5. **pingcang.py 跑完 get_storage() 返回 "UNKNOWN"**：任意角度不属于 LEFT/RIGHT 两档，预期行为，不是 bug。

### 4.2 待办（按优先级）

| 序 | 事项 | 优先级 |
| --- | --- | --- |
| 1 | 现场跑 `python main/arm/each_task/task7/pingcang.py` 验证 v2 修复 | 高 |
| 2 | 现场跑 `python main/arm/each_task/task7/get_position2.py` 验证 4 步序列 | 高 |
| 3 | 用户现场给 6 位置的 x_mm / y_mm 后，建 position4/6.py | 高 |
| 4 | 现场标定储存仓 raw 协议值（找"开仓最大开角"） | 中 |
| 5 | 如果位置间需要按特定顺序扫（不是默认 1-2-3-4-5-6），改 `POSITIONS` 列表顺序 | 低 |
| 6 | 如果 `target.py` setup 的 y=-80 跟某位置 y 不一致，按现场实测统一 | 中 |

### 4.3 还没解决的疑问

- **pingcang.py 的 90° 现场验证**：v2 修复后应该 work，但 90° 物理位置是否合理要看实际舵机机械结构。
- **position5 v2 7 步 vs v3 9 步**：今天梳理的 9 步是 v3（含 Phase C 撤退），v1/v2 历史见 debug-task7-position2-2026-08-03.md §2.7。
- **每个位置是否真的需要 OCR 一次**？目前是 1×OCR（cam2 一帧拍全）。如果现场发现一帧拍不全，可能要 6×OCR。

---

## 5. 业务层硬约束（[[arm-business-layer-only]]）

**严禁改动**：
- `smartcar/whalesbot/**`
- `runtime/**`
- `car_wrap_2026.py`
- `car_start_2026.py`
- `car_task_function.py`

**可以改**：
- `main/**`（业务层）
- 包括 `main/arm/each_task/task7/*` ← 当前工作面

---

## 6. 业务层相关 runtime 行为（**只读不改**, 但要理解）

### 6.1 runtime 并发模型（2026-07-16 改造后）

- **`_ref_lock`** —— 微秒级，保护 `self.car` 引用替换
- **`_realtime_gate`** —— 微秒级，realtime 端点（`/v1/realtime/*`）入口
- 旧 `car_lock`（RLock 锁全程）改成抛 `RuntimeError` 的 property
- **`job_queue` 拆 `arm_queue` + `car_queue`** 两个独立 worker

### 6.2 `/v1/execute` 默认异步

- 默认 `sync=False` 立即返回 `job_id`，状态查 `/v1/jobs/{id}`
- 业务层要拿 result 必须 `sync=True`（[[execute-sync-default]]）
- 否则 `result=None` → `float(None)` TypeError

### 6.3 ARM_API.md 关键变化（2026-08-01）

- **x 轴软限位 [x_min_m, x_max_m] 由 `arm_cfg.yaml` 配置**：当前 `x_min_m=-0.32` / `x_max_m=0.22`（0~-320mm + 0~+220mm 全段）
- **`ArmOrigin.soft_x_min/max_mm` 字段保留但固定 None**
- **`move_x_position` 移除 `x_stop_check` 中途 calibrate** —— exit 条件只剩 PID 收敛 + `out_time` 超时
- **`reset_x` 完全重写** —— 单档正向驱动 + 窗口制 stall 检测（STALL_WINDOW_S=0.3s, 阈值 1mm, 连续 3 窗口不动=撞墙）
- **`STOP_CHECK_THRESHOLD=1e-10` → `1e-3`**（commit `5549ea3`）

### 6.4 arm_feed / lane_feed 守护线程

- 默认 `RAK_CAR_AUTO_INIT=1`，init 时自动启 20Hz
- 不抢 car_lock（走 meta_lock）
- `lane_state.active=False` → `_stop_flag=True` → daemon break 退出
- **清 stop 必须 POST `/v1/control/reset-stop` + 重启 lane_feed**

---

## 7. 关键文件路径速查

```
repo root: C:\Users\29368\Desktop\智能车\rak-car\
工作面:   main/arm/each_task/task7/  (target.py / dipan.py / position1-5.py / get_position1-2.py / pingcang.py)
共享:    main/arm/each_task/common.py:move_x_with_split
         main/arm/api/setters.py:ArmClient.set_hand_angle
         main/arm/api/storage.py:ArmClient.set_storage_angle
         main/arm/loops/runner.py:ArmRunner.set_arm_angle / move_y (无 set_hand_angle)
配置:    main/misc/llm_config.yml  (ERNIE token)
         config_car.yml (ernie_access_token 字段, fallback)
         main/settings.py:9  (DEFAULT_SERVER_ORIGIN = "http://192.168.5.230" ← 当前 Jetson IP)
         smartcar/whalesbot/vehicle/arm/arm_cfg.yaml  (x_min_m / x_max_m)
```

---

## 8. 相关 memory 索引

### 8.1 通用 / runtime

- [[arm-api-reference]] —— ARM_API.md 速查
- [[arm-business-layer-only]] —— 业务层边界硬约束
- [[execute-sync-default]] —— /v1/execute 默认 sync=False
- **[[execute-car-action-args-pattern]]** —— **本 session 新加**，execute_car_action args 包裹规则（单参 list vs 多参）
- [[arm-grasp-call-arm-base]] —— grasp 调用坑
- [[armrunner-set-hand-angle-gotcha]] —— **本 session 引用**: runner 没 set_hand_angle, 必须 client.set_hand_angle + timeout 必填

### 8.2 arm / x 轴

- [[x-axis-rollout-session]] —— x 轴全天 session
- [[x-axis-belt-slip]] —— x 轴 belt-slip 现象
- [[x-get-position-vs-realtime]] —— x_get_position 坏, 走 realtime
- [[x-speed-safety-watchdog]] —— x_speed 后台 watchdog
- [[x-stop-check-threshold-2026-08-01]] —— x_stop_check 阈值修复

### 8.3 task / 业务

- [[task7-position2-2026-08-03]] —— 上一 session 上下文（task7 各文件状态/位置常量/历史改动轨迹）
- [[task4-rebuild-2026-07-22]] / [[task5-rebuild-2026-07-22]] —— 历史任务压缩文档参考格式
- [[target2-teachpoint-2026-07-27]] / [[ball-verified-2026-07-28-recalibration]] / [[ball-best-grasp-2026-07-29]] / [[ball-best-grasp-2026-07-30]] —— task1-4 球抓取坐标基线
- [[target4-adjust-state-machine]] —— target4 状态机
- [[color-tag-roi-calibration]] —— task5 高仓色标 v3 收尾

### 8.4 基础设施

- [[jetson-current-ip]] —— 当前 Jetson IP 192.168.5.230
- [[stream-cam-id-mapping]] —— cam2 = 侧摄 (config 与 stream URL 编号不一致)

---

## 9. 续接指南（下次会话第一件事）

1. **读本文档**（你已经做了）
2. **读 [[arm-business-layer-only]]** 确认业务层边界（不能动 runtime / smartcar SDK）
3. **读 [[execute-car-action-args-pattern]]**（本 session 新加, 通用踩坑, 写新 car/arm action 调用前必看）
4. **读 [[armrunner-set-hand-angle-gotcha]]**（runner 没 set_hand_angle, 必须 client.set_hand_angle + timeout 必填）
5. **读 [[task7-position2-2026-08-03]]** 看上一 session 上下文（task7 各文件当前位置常量）
6. **检查 task7 目录当前状态**：`ls main/arm/each_task/task7/` 看是否多了其它文件（用户中途可能改）
7. **跑 `python main/arm/each_task/task7/pingcang.py` 验证 v2 修复**（连 runtime + servo_1 是否就绪）
8. **如需 OCR 验证**：跑 `python main/arm/each_task/task7/target.py`（连 runtime + cam2 + ERNIE token 是否就绪）

---

## 10. 一句话总结

今天修了 2 个 task7 文件（get_position2 v2 / pingcang v2），挖出 1 个**通用踩坑**（execute_car_action args 包裹规则，已写入独立 memory）。任务 7 还差 position4/6 两个位置文件未建，等用户给坐标。下一 session 入口：现场跑 pingcang 验证 v2，然后等用户给 position4/6 坐标。