# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ROS2 / colcon
install/
log/
build/
*.colcon*
*.bag
*.bag.active

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Project-specific logs / runtime data
*.log
log_info/
.remember/
/Overload
.pubrak

# Screenshots / debug images (keep docs/ ones)
*.png
!docs/**/*.png
!docs/**/*.jpg

# C++ build artifacts
*.o
*.a
*.so
*.so.*
*.dep
*.d

# Reference package carried over from main (kept locally, not in git)
baidu_smartcar_2026.zip
fishros
reference/

# Test / coverage
.pytest_cache/
*.gcov
*.gcda
*.gcno
coverage.xml

# Calibration session artifacts (operator-only, regenerated each run)
calibration_session/


# Code intelligence index (per-machine)
.codegraph/

# Serena editor state (per-machine)
**/.serena/

# ROS2 colcon artifacts inside package src (workspace = repo root)
src/*/AMENT_IGNORE
src/*/CMakeFiles/
