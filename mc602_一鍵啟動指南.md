# MC602 純下位機一鍵啟動完整指南

> 適用場景：比賽規則不允許攜帶電腦時，僅靠 MC602 控制器按鍵選擇並自動執行完整任務

---

## 目錄

1. [架構總覽](#1-架構總覽)
2. [現有啟動機制分析](#2-現有啟動機制分析)
3. [修改現有代碼實現一鍵啟動](#3-修改現有代碼實現一鍵啟動)
4. [完整任務流程封裝](#4-完整任務流程封裝)
5. [運行與測試](#5-運行與測試)
6. [常見問題排查](#6-常見問題排查)
7. [進階：其他實現方法參考](#7-進階其他實現方法參考)

---

## 1. 架構總覽

### 1.1 系統分層

```
┌─────────────────────────────────────────┐
│              Jetson Nano                 │
│         （上位機 - 目前運行位置）           │
│  ┌─────────────────────────────────┐    │
│  │  car_start_2026.py              │    │
│  │  car_task_function.py           │    │
│  │  car_wrap_2026.py (MyCar 類)    │    │
│  │  config_car.yml                 │    │
│  │                                 │    │
│  │  import smartcar.whalesbot.*    │    │
│  │  → 透過序列埠 / USB 與 MC602 通訊│    │
│  └─────────────────────────────────┘    │
└──────────────────┬──────────────────────┘
                   │ USB / 序列埠
                   ▼
┌─────────────────────────────────────────┐
│           MC602 控制器                    │
│         （下位機 - 目標部署位置）           │
│                                         │
│  ┌──────────┐  ┌──────────────────┐    │
│  │ MC602 固件│  │  內部 Flash 存儲   │    │
│  │ Run.bin   │  │  程序槽位 APP1~6  │    │
│  └──────────┘  └──────────────────┘    │
│                                         │
│  控制能力：馬達 ×4、編碼器、機械臂、       │
│  蜂鳴器、屏幕、按鍵 ×4、藍牙手柄、        │
│  舵機、步進馬達、傳感器等                  │
└─────────────────────────────────────────┘
```

### 1.2 關鍵代碼位置

| 文件 | 路徑 | 用途 |
|------|------|------|
| 主啟動 | `car_start_2026.py` | 目前用來啟動全部任務 |
| 任務函數 | `car_task_function.py` | 8 個比賽任務的 Python 實現 |
| 車輛控制 | `car_wrap_2026.py` | `MyCar` 類，封裝所有底層操作 |
| 硬體配置 | `config_car.yml` | PID、攝像頭、IO、推理端口 |
| 燒錄工具 | `smartcar/whalesbot/vehicle/base/pydownload.py` | 把 Run.bin 燒到 MC602 |
| 控制器驅動 | `smartcar/whalesbot/vehicle/base/mc602_ctl2.py` | MC602 協議與設備抽象 |
| 序列埠封裝 | `smartcar/whalesbot/vehicle/base/serial_wrap.py` | 自動偵測並連接 MC602 |
| 機械臂基礎 | `smartcar/whalesbot/vehicle/arm/arm_base.py` | 機械臂、舵機、傳感器等封裝 |

---

## 2. 現有啟動機制分析

### 2.1 MC602 內存佈局（Flash Map）

來源：`smartcar/whalesbot/vehicle/base/pydownload.py`

```python
FLASH_BASE        = 0x08000000
PROGRAM_GCC       = FLASH_BASE + (52*1024)    # 0x0800C000  ← Run.bin 主要運行區
MC602_APP1_ADDRESS = FLASH_BASE + (384*1024)  # 0x08060000
MC602_APP2_ADDRESS = FLASH_BASE + (512*1024)  # 0x08080000
MC602_APP3_ADDRESS = FLASH_BASE + (612*1024)  # 0x08099000
MC602_APP4_ADDRESS = FLASH_BASE + (712*1024)  # 0x080B2000
MC602_APP5_ADDRESS = FLASH_BASE + (812*1024)  # 0x080CB000
MC602_APP6_ADDRESS = FLASH_BASE + (912*1024)  # 0x080E4000
```

**含義：**
- MC602 內置 Flash 可以存 6 個用戶程序（APP1~APP6）
- `PROGRAM_GCC` 是默認運行區，燒錄後立刻執行
- `Run.bin` 是編譯好的 MC602 固件二進制文件

### 2.2 目前的燒錄流程

```python
# smartcar/whalesbot/vehicle/base/serial_wrap.py
# MC602.download_bin() 方法

1. 序列埠偵測到 MC602 → 進入 BootLoader 模式
2. 發送 0x55AA 頭幀 → MC602 返回確認
3. 發送 RAM2FLASH 指令 → MC602 進入下載狀態
4. Python 調用 Scratch_Download_MC602P("RunA", isrun=True)
   → 讀取 Run.bin
   → 以 4KB 為單位發送數據
   → CRC32 校驗每段
   → 寫入 Flash
5. 燒錄完成後自動運行 Run.bin
6. Run.bin 啟動後，初始化序列埠、等待上位機指令
```

### 2.3 目前的啟動流程（需要電腦）

```
開機 Jetson
    ↓
登入系統
    ↓
執行：python car_start_2026.py
    ↓
MyCar.__init__() 初始化
    ├── 連接 MC602（序列埠 / USB）
    ├── 初始化攝像頭
    ├── 初始化 Paddle 推理服務
    ├── 初始化文心一言 API
    ├── 啟動按鍵監聽線程
    └── 蜂鳴器提示初始化完成
    ↓
main() 依序執行 8 個任務
    ├── init()
    ├── auto_seeding()
    ├── target_shooting_detection()
    ├── water_tower_task()
    ├── target_shooting()
    ├── crop_harvesting()
    ├── sort_and_store()
    ├── get_order()
    └── order_delivery()
```

---

## 3. 修改現有代碼實現一鍵啟動

### 3.1 目標

讓 MC602 屏幕上顯示任務選單，按一個鍵就自動跑完全部任務，不需要電腦。

### 3.2 關鍵：`MyCar.manage()` 方法

來源：`smartcar/whalesbot/vehicle/base/car_wrap_2026.py` 第 1657 行

```python
def manage(self, programs_list: list, order_index=0):
    """
    程序管理方法 - 這是一個選單式啟動框架
    
    參數:
        programs_list: 程序列表，包含要執行的函數
        order_index: 初始選中的程序索引，預設為 0
    """
```

**內部已經實現了：**
- 屏幕顯示程序列表（最多 5 個一頁）
- 按鍵 2/4 上下選擇
- 按鍵 3 確認執行
- 長按按鍵 1 退出程序

**內置的 3 個默認選項：**
```python
programs_suffix = [all_task, lane_test, self.debug]
programs = programs_list.copy()
programs.extend(programs_suffix)
```

### 3.3 步驟一：把 8 個任務封裝成一個函數

創建新文件 `car_auto_run.py`：

```python
#!/usr/bin/python3
# -*- coding: utf-8 -*-
"""
純下位機一鍵啟動腳本
功能：把 8 個比賽任務封裝成一個函數，讓 MC602 按鍵選擇後自動執行
"""
import os
import sys
import time
import math

# 確保能找到模塊
sys.path.append(os.path.abspath(os.path.dirname(__file__)))

from car_wrap_2026 import MyCar, kill_other_python
from car_task_function import (
    init,
    auto_lane_tracing,
    auto_seeding,
    target_shooting_detection,
    water_tower_task,
    target_shooting,
    crop_harvesting,
    sort_and_store,
    get_order,
    order_delivery
)

# 全局車輛實例
my_car = None

def run_all_tasks():
    """
    完整任務流程 - 這是被 manage() 調用的主函數
    
    順序：
    1. 系統初始化
    2. 自動播種
    3. 蟲害偵察
    4. 水塔灌溉
    5. 射擊除害
    6. 作物採收
    7. 分類儲存
    8. 獲取訂單 + 訂單配送
    """
    global my_car
    
    try:
        # ===== 任務 1：初始化 =====
        my_car.beep()
        time.sleep(0.5)
        init()                                          # 初始化
        my_car.beep()
        
        # ===== 任務 2：自動播種 =====
        my_car.beep()
        time.sleep(0.5)
        auto_seeding()                                  # 播種任務
        my_car.beep()
        
        # ===== 任務 3：蟲害偵察 =====
        my_car.beep()
        time.sleep(0.5)
        animal_list = target_shooting_detection()       # 識別蟲害
        my_car.beep()
        
        # ===== 任務 4：水塔灌溉 =====
        my_car.beep()
        time.sleep(0.5)
        water_tower_task()                              # 灌溉任務
        my_car.beep()
        
        # ===== 任務 5：射擊除害 =====
        my_car.beep()
        time.sleep(0.5)
        target_shooting(animal_list)                    # 射擊除害
        my_car.beep()
        
        # ===== 任務 6：作物採收 =====
        my_car.beep()
        time.sleep(0.5)
        crop_harvesting()                               # 作物收集
        my_car.beep()
        
        # ===== 任務 7：分類儲存 =====
        my_car.beep()
        time.sleep(0.5)
        sort_and_store()                                # 作物儲存
        my_car.beep()
        
        # ===== 任務 8：訂單配送 =====
        my_car.beep()
        time.sleep(0.5)
        order_list = get_order()                        # 訂單獲取
        order_delivery(order_list)                      # 訂單配送
        my_car.beep()
        
        # ===== 全部完成 =====
        # 連續蜂鳴 3 次表示全部完成
        for _ in range(3):
            my_car.beep()
            time.sleep(0.3)
        
        print("所有任務已完成！")
        
    except Exception as e:
        # 出錯時蜂鳴器長鳴提示
        print(f"任務執行出錯: {e}")
        import traceback
        traceback.print_exc()
        if my_car is not None:
            for _ in range(5):
                my_car.beep()
                time.sleep(0.2)

def main():
    """
    主入口 - 使用 manage() 提供選單界面
    """
    global my_car
    
    # 清理可能殘留的 Python 進程
    kill_other_python()
    time.sleep(1)
    
    # 初始化車輛
    my_car = MyCar()
    my_car.STOP_PARAM = False  # 关闭按键停止功能（可选）
    
    # 等待系統穩定
    time.sleep(2)
    
    # 把完整任務包裝成列表
    all_task_func = run_all_tasks
    
    # 可以添加其他單獨測試的任務
    test_tasks = [
        # auto_seeding,           # 單獨測試播種
        # target_shooting_detection,  # 單獨測試蟲害偵察
        # water_tower_task,       # 單獨測試灌溉
        # target_shooting,        # 單獨測試射擊
        # crop_harvesting,        # 單獨測試採收
        # sort_and_store,         # 單獨測試分類
        # get_order,              # 單單測試訂單
        # order_delivery,         # 單獨測試配送
    ]
    
    # 使用 manage() 啟動選單
    # manage() 內含的默認選項：
    #   - all_task:    執行 run_all_tasks
    #   - lane_test:   巡線測試
    #   - debug:       遙控器調試模式
    my_car.manage([all_task_func] + test_tasks, order_index=0)

if __name__ == "__main__":
    main()
```

### 3.4 步驟二：修改 car_start_2026.py（可選）

如果你想保留原來的啟動方式，可以把 `car_start_2026.py` 改成調用上面的腳本：

```python
#!/usr/bin/python3
# -*- coding: utf-8 -*-
import os
import sys

# 如果是比賽模式（純下位機），取消下面的註釋
# from car_auto_run import main
# main()

# 如果是調試模式（需要電腦），保留原來的邏輯
sys.path.append(os.path.abspath(os.path.dirname(__file__)))

from car_wrap_2026 import MyCar
from car_task_function import (
    init,
    auto_seeding,
    target_shooting_detection,
    water_tower_task,
    target_shooting,
    crop_harvesting,
    sort_and_store,
    get_order,
    order_delivery
)

def main():
    my_car = MyCar()
    my_car.STOP_PARAM = False
    init()
    auto_seeding()
    animal_list = target_shooting_detection()
    water_tower_task()
    target_shooting(animal_list)
    crop_harvesting()
    sort_and_store()
    order_list = get_order()
    order_delivery(order_list)

if __name__ == "__main__":
    main()
```

### 3.5 步驟三：確認屏幕顯示正常

`MyCar.manage()` 會自動在 MC602 的屏幕上顯示：

```
>>1.run_all_tasks    ← 當前選中（">>" 標記）
  2.auto_seeding
  3.target_shooting_detection
  4.lane_test
  5.debug
```

**按鍵說明（MC602 控制器上的 4 個按鍵）：**

| 按鍵 | 短按 | 長按（>0.7秒） |
|------|------|--------------|
| 按鍵 1 | 無（或上一頁） | 退出程序 |
| 按鍵 2 | 向上選擇 | 無 |
| 按鍵 3 | 確認執行 | 無 |
| 按鍵 4 | 向下選擇 | 無 |

---

## 4. 完整任務流程封裝

### 4.1 run_all_tasks() 的執行順序

```
run_all_tasks()
    │
    ├── [1] init()
    │       ├── 蜂鳴器提示
    │       ├── 機械臂復位
    │       └── 里程計清零
    │
    ├── [2] auto_seeding()
    │       ├── 巡線到播種區
    │       ├── 識別並記錄 3 個播種點位置
    │       ├── 從播種台取種子
    │       └── 播種到 3 個位置
    │
    ├── [3] target_shooting_detection()
    │       ├── 巡線到蟲害偵察區
    │       ├── 逐個識別 4 個動物靶標
    │       ├── 使用文心一言分析動物類型
    │       └── 返回動物列表 [害蟲/益蟲, ...]
    │
    ├── [4] water_tower_task()
    │       ├── 巡線到水塔區
    │       ├── 識別 2 個水塔（記錄水量需求）
    │       ├── 記錄 6 個水塊位置
    │       └── 逐個水塔澆水（根據水量需求）
    │
    ├── [5] target_shooting(animal_list)
    │       ├── 巡線到射擊區
    │       ├── 根據動物列表確定需要射擊的位置
    │       └── 逐個移動到目標並射擊
    │
    ├── [6] crop_harvesting()
    │       ├── 巡線到採收區
    │       ├── 逐個識別 8 個果實（黃球/藍球）
    │       └── 吸取果實到儲存架
    │
    ├── [7] sort_and_store()
    │       ├── 巡線到分類區
    │       ├── 讀取顏色標籤
    │       └── 按顏色分揀到存儲倉
    │
    └── [8] order_delivery(get_order())
            ├── 巡線到訂單區
            ├── OCR 識別訂單信息
            ├── 文心一言解析訂單
            ├── 按地址排序
            ├── 依次取貨
            └── 送到對應住戶格口
```

### 4.2 單獨測試某個任務

如果某個任務需要單獨調試，可以在 `main()` 中修改：

```python
def main():
    my_car = MyCar()
    my_car.STOP_PARAM = False
    
    # 只測試單個任務
    init()
    time.sleep(1)
    
    # 取消註釋來測試單個任務
    # auto_seeding()
    # target_shooting_detection()
    # water_tower_task()
    # target_shooting([1, 0, 1, 0])  # 手動指定動物列表
    # crop_harvesting()
    # sort_and_store()
    # get_order()
    # order_delivery([{"name": "測試", "goods": "芹菜", "address": 1}])
```

---

## 5. 運行與測試

### 5.1 在 Jetson 上運行（調試階段）

```bash
# 進入項目目錄
cd /home/jetson/workspace/baidu_smartcar_2026

# 運行選單式啟動
python3 car_auto_run.py
```

**預期行為：**
1. 終端輸出連接日誌
2. MC602 屏幕顯示選單
3. 按鍵 3 選擇 run_all_tasks
4. 任務自動執行
5. 完成後蜂鳴提示，回到選單

### 5.2 燒錄到 MC602 並脫離電腦運行

#### 方法一：使用 pydownload.py 命令行

```bash
cd smartcar/whalesbot/vehicle/base

# 燒錄並直接運行
python3 pydownload.py
```

#### 方法二：使用 Python API

```python
from smartcar.whalesbot.vehicle.base.pydownload import Scratch_Download_MC602P

# 燒錄到 RunA 槽位並自動運行
result, msg = Scratch_Download_MC602P("RunA", isrun=True)
print("燒錄結果:", result)
print("日誌:", msg)
```

#### 方法三：直接命令行

```bash
python3 -c "
from smartcar.whalesbot.vehicle.base.pydownload import Scratch_Download_MC602P
Scratch_Download_MC602P('RunA', isrun=True)
"
```

### 5.3 燒錄後脫離電腦測試

1. 燒錄完成後，MC602 會自動運行 Run.bin
2. 關閉 Jetson 或拔掉 USB
3. 用移動電源為 Jetson 供電
4. 打開車的電源開關
5. Jetson 開機後自動啟動 car_auto_run.py（如果配置了開機自啟）
6. MC602 屏幕顯示選單
7. 按鍵 3 開始任務

### 5.4 配置 Jetson 開機自啟（可選但推薦）

```bash
# 1. 創建 systemd 服務文件
sudo nano /etc/systemd/system/smartcar.service
```

內容：
```ini
[Unit]
Description=SmartCar 2026 Auto Start
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/jetson/workspace/baidu_smartcar_2026
ExecStart=/usr/bin/python3 car_auto_run.py
Restart=always
RestartSec=5
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=multi-user.target
```

啟用服務：
```bash
sudo systemctl daemon-reload
sudo systemctl enable smartcar.service
sudo systemctl start smartcar.service
```

查看狀態：
```bash
sudo systemctl status smartcar.service
```

停止服務：
```bash
sudo systemctl stop smartcar.service
```

---

## 6. 常見問題排查

### 6.1 MC602 連接失敗

**現象：** 啟動時一直顯示「未接控制器」

**排查：**
1. 檢查 USB 線是否連接牢固
2. 檢查 MC602 是否開機（屏幕是否亮起）
3. 檢查 CH340 驅動是否安裝

```bash
# 查看串口列表
ls /dev/ttyUSB*

# 查看驅動
lsmod | grep ch340
```

### 6.2 攝像頭無法打開

**現象：** 報錯 `camera init fail`

**排查：**
1. 檢查攝像頭連接
2. 檢查設備號是否正確（`config_car.yml` 中 front: 1, side: 2）

```bash
# 查看視頻設備
ls /dev/video*

# 測試攝像頭
ffmpeg -f v4l2 -list_formats all -i /dev/video1
```

### 6.3 任務中車輛停止

**原因：** `_stop_flag` 被觸發

**可能原因：**
1. 按下了 MC602 的按鍵 3（觸發停止）
2. 程序異常退出
3. 某個任務函數內部返回了

**排查：**
```bash
# 查看程序輸出日誌
tail -f /var/log/syslog | grep smartcar
```

### 6.4 屏幕顯示異常

**現象：** MC602 屏幕顯示亂碼或空白

**排查：**
1. 檢查 `ScreenShow_2` 的初始化
2. 確保字符編碼是 UTF-8

### 6.5 燒錄失敗

**現象：** `Download Fail` 或 `RunCode Fail`

**排查：**
1. 確保 MC602 處於 BootLoader 模式
2. 檢查 `Run.bin` 是否存在且完整
3. 嘗試更高的打印級別：`PRINT_LEVEL = 20`

---

## 7. 進階：其他實現方法參考

### 7.1 方法一：直接修改 manage() 的默認選項

如果想讓屏幕上只有一個「開始比賽」選項，可以修改 `MyCar.manage()` 方法：

```python
# car_wrap_2026.py
def manage(self, programs_list: list, order_index=0):
    def all_task():
        time.sleep(4)
        # 直接跑全部任務，不需要選單
        from car_auto_run import run_all_tasks
        run_all_tasks()
    
    def lane_test():
        self.lane_dis_offset(0.3, 30)
    
    # 只保留這兩個選項
    programs_suffix = [all_task, lane_test]
    programs = []
    programs.extend(programs_suffix)
    # ... 後續不變
```

### 7.2 方法二：使用 autostart 文件

MC602 的固件可能支持特定文件名的自動運行。查看 `pydownload.py`：

```python
# 如果文件命名為特定名稱，MC602 上電後自動運行
# 例如 "auto_run.bin" 或 "boot.bin"
```

可以嘗試：
```python
Scratch_Download_MC602P("RunA", isrun=True)  # RunA 可能是默認自動運行
```

### 7.3 方法三：藍牙手柄觸發

如果比賽允許使用藍牙手柄，可以在 `manage()` 中添加手柄觸發邏輯：

```python
# 在 manage() 中添加
while True:
    btn = self.key.get_key()
    if btn == 3:  # 按鍵 3 開始
        run_all_tasks()
        break
```

### 7.4 方法四：電源觸發（需要硬件改裝）

如果 MC602 支持電源觸發引腳，可以：
1. 把車的電源開關連接到某個 GPIO
2. MC602 上電後檢測 GPIO 狀態
3. 自動開始運行任務

這需要修改 MC602 固件源碼（C 語言），不在此範疇。

### 7.5 方法五：定時器觸發

如果任務有嚴格時間限制，可以添加倒計時功能：

```python
import time

def run_with_countdown(delay_seconds=10):
    """倒計時後自動開始任務"""
    for i in range(delay_seconds, 0, -1):
        my_car.display.show(f"開始倒計時\n{i} 秒後執行")
        my_car.beep()
        time.sleep(1)
    run_all_tasks()
```

---

## 附錄 A：快速操作 checklist

### 日常調試流程

```
□ 1. USB 連接 Jetson 和 MC602
□ 2. 開啟 Jetson 終端
□ 3. cd /home/jetson/workspace/baidu_smartcar_2026
□ 4. python3 car_auto_run.py
□ 5. 等待屏幕顯示選單
□ 6. 按鍵 3 選擇 run_all_tasks
□ 7. 觀察任務執行
□ 8. 長按按鍵 1 退出
```

### 比賽日流程

```
□ 1. 前一天燒錄好 Run.bin（見 5.2 節）
□ 2. 檢查所有連接（USB、攝像頭、電源）
□ 3. 開啟車的電源
□ 4. Jetson 開機（等待約 30-60 秒）
□ 5. 確認 MC602 屏幕顯示選單
□ 6. 按鍵 3 開始任務
□ 7. 觀察車輛運行
□ 8. 任務完成後蜂鳴提示
```

### 燒錄流程

```
□ 1. 修改代碼後，確認 car_auto_run.py 正常
□ 2. cd smartcar/whalesbot/vehicle/base
□ 3. python3 -c "from pydownload import Scratch_Download_MC602P; Scratch_Download_MC602P('RunA', isrun=True)"
□ 4. 等待燒錄完成
□ 5. MC602 自動運行
□ 6. 測試選單是否正常顯示
```

---

## 附錄 B：重要配置文件說明

### config_car.yml 關鍵部分

```yaml
# 攝像頭配置
camera:
  front: 1    # 前置攝像頭通道
  side: 2     # 側面攝像頭通道

# IO 配置
io:
  key: 1      # 按鍵端口（MC602 上的按鍵模塊）
  light: 2    # 燈光端口

# 速度限制
speed:
  x: { limit: 0.7 }   # 橫向速度 m/s
  y: { limit: 0.7 }   # 縱向速度 m/s
  angle: { limit: 3 }  # 角速度 rad/s

# PID 參數
lane_pid:
  cfg_pid_y: { Kp: 5, Ki: 0.1, Kd: 0 }
  cfg_pid_angle: { Kp: 3, Ki: 0, Kd: 0 }

# 推理服務端口
infer_cfg:
  - name: lane
    port: 5001
  - name: task
    port: 5002
  - name: ocr
    port: 5004

# 文心一言 Token（需要網絡）
ernie_access_token: YOUR_TOKEN_HERE
```

**注意：** 如果比賽現場沒有網絡，文心一言 API 會超時失敗。需要：
1. 預先測試哪些任務依賴 API
2. 或者準備離線備用方案

---

## 附錄 C：任務依賴關係圖

```
init()
  │
  ├── auto_seeding() ─────────────────────┐
  │       │                               │
  │       └── auto_lane_tracing()         │
  │                                       │
  ├── target_shooting_detection() ────────┤
  │       │                               │
  │       └── animal_image_analysis()     │
  │           └── ErnieBot API (雲端)     │
  │                                       │
  ├── water_tower_task() ─────────────────┤
  │       │                               │
  │       └── move_to_detection_target()  │
  │                                       │
  ├── target_shooting(animal_list) ───────┤
  │       │                               │
  │       └── lane_dis_offset()           │
  │                                       │
  ├── crop_harvesting() ──────────────────┤
  │       │                               │
  │       └── get_detection_results()     │
  │                                       │
  ├── sort_and_store() ───────────────────┤
  │       │                               │
  │       └── get_detection_results()     │
  │                                       │
  ├── get_order() ────────────────────────┤
  │       │                               │
  │       ├── get_ocr() ──────────────────┤
  │       └── order_analysis.get_res_json()│
  │           └── ErnieBot API (雲端)     │
  │                                       │
  └── order_delivery(order_list) ─────────┘
          │
          └── find_name() ────────────────┐
                  │                        │
                  └── get_detection_results()│
                  └── get_det_ocr()        │
                                          │
全部任務完成 → 蜂鳴提示 → 回到選單       │
```

---

## 附錄 D：文件清單

修改/創建的文件：

```
/home/jetson/workspace/baidu_smartcar_2026/
├── car_auto_run.py          ← 新創建：純下位機啟動腳本
├── car_start_2026.py        ← 可選修改：保留原啟動方式
├── car_task_function.py     ← 不需要修改
├── car_wrap_2026.py         ← 不需要修改
├── config_car.yml           ← 不需要修改
├── smartcar/
│   └── whalesbot/
│       └── vehicle/
│           ├── base/
│           │   ├── pydownload.py           ← 燒錄工具（已存在）
│           │   ├── Run.bin                 ← 燒錄文件（已存在）
│           │   ├── mc602_ctl2.py           ← 控制器驅動（已存在）
│           │   └── serial_wrap.py          ← 序列埠封裝（已存在）
│           └── arm/
│               └── arm_base.py            ← 機械臂驅動（已存在）
└── mc602_一鍵啟動指南.md     ← 本文檔
```

---

## 文檔版本

- 版本：1.0
- 創建日期：2026-08-08
- 適用項目：百度智能車 2026
- 適用控制器：MC602
